; Declared theme capabilities (architecture-principles.md §30 -- read by
; UI::RmlBridge::ThemeProvidesOwnIconChrome(), RmlTheme.cpp). Optional: a theme that doesn't need
; this file gets the safe default (0/false) for every key. See NewUIMainFrameWindow.cpp's call
; sites for what this specific key controls.
[Capabilities]
ProvidesOwnIconChrome=1
