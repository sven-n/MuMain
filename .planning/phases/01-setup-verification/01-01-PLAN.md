---
phase: 01-setup-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
user_setup:
  - service: OpenMU
    why: "Manual verification requires server running"
    note: "User has OpenMU on Arch Linux VM at 192.168.3.34"

must_haves:
  truths:
    - "x86 Release builds successfully"
    - "x64 Release builds successfully"
    - "x86 MuEditor Release builds successfully"
    - "x64 MuEditor Release builds successfully"
    - "User confirms game launches and connects to OpenMU at 192.168.3.34"
  artifacts:
    - path: "out/build/windows-x86/src/Release/Main.exe"
      provides: "x86 Release executable"
    - path: "out/build/windows-x64/src/Release/Main.exe"
      provides: "x64 Release executable"
    - path: "out/build/windows-x86-mueditor/src/Release/Main.exe"
      provides: "x86 MuEditor Release executable"
    - path: "out/build/windows-x64-mueditor/src/Release/Main.exe"
      provides: "x64 MuEditor Release executable"
  key_links: []
---

<objective>
Configure and build all four Release variants (x86, x64, MuEditor x86, MuEditor x64), then have user manually verify the game launches and connects to OpenMU.

Purpose: Verify the build pipeline on Windows 11 with CMakePresets + Ninja before proceeding to Phase 2.
Output: Built Main.exe for each variant; user confirmation that game runs.
</objective>

<execution_context>
@C:\Users\Uburrato\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Uburrato\.claude/get-shit-done/templates/summary.md
@C:\Users\Uburrato\.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-setup-verification/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure and build x86 Release</name>
  <files>CMakePresets.json</files>
  <action>From repo root, run: cmake --preset windows-x86. Then run: cmake --build --preset windows-x86-release. Ensure no errors. Main.exe will be in out/build/windows-x86/src/Release/</action>
  <verify>Test -Path "out/build/windows-x86/src/Release/Main.exe" -PathType Leaf</verify>
  <done>x86 Release Main.exe exists</done>
</task>

<task type="auto">
  <name>Task 2: Configure and build x64 Release</name>
  <files>CMakePresets.json</files>
  <action>Run: cmake --preset windows-x64. Then run: cmake --build --preset windows-x64-release. Ensure no errors. Main.exe in out/build/windows-x64/src/Release/</action>
  <verify>Test -Path "out/build/windows-x64/src/Release/Main.exe" -PathType Leaf</verify>
  <done>x64 Release Main.exe exists</done>
</task>

<task type="auto">
  <name>Task 3: Configure and build x86 MuEditor Release</name>
  <files>CMakePresets.json</files>
  <action>Run: cmake --preset windows-x86-mueditor. Then run: cmake --build --preset windows-x86-mueditor-release. Main.exe in out/build/windows-x86-mueditor/src/Release/</action>
  <verify>Test -Path "out/build/windows-x86-mueditor/src/Release/Main.exe" -PathType Leaf</verify>
  <done>x86 MuEditor Release Main.exe exists</done>
</task>

<task type="auto">
  <name>Task 4: Configure and build x64 MuEditor Release</name>
  <files>CMakePresets.json</files>
  <action>Run: cmake --preset windows-x64-mueditor. Then run: cmake --build --preset windows-x64-mueditor-release. Main.exe in out/build/windows-x64-mueditor/src/Release/</action>
  <verify>Test -Path "out/build/windows-x64-mueditor/src/Release/Main.exe" -PathType Leaf</verify>
  <done>x64 MuEditor Release Main.exe exists</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Four Main.exe variants built. Pick one (e.g. out/build/windows-x86/src/Release/Main.exe) and run from its directory or pass correct working dir. Connection: Main.exe connect /u192.168.3.34 /p44406</what-built>
  <how-to-verify>Launch Main.exe with: Main.exe connect /u192.168.3.34 /p44406 (from the exe's directory, or ensure src/bin assets are present). Confirm: 1) Game window opens 2) Connects to OpenMU 3) No crash</how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- [ ] All four Main.exe files exist
- [ ] User approved manual launch/connect test
</verification>

<success_criteria>
- All four build variants complete without error
- User confirms game launches and connects to OpenMU
- Phase 1 goal achieved
</success_criteria>

<output>
After completion, create .planning/phases/01-setup-verification/01-01-SUMMARY.md
</output>
